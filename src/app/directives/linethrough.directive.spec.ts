import { LinethroughDirective } from './linethrough.directive';

describe('LinethroughDirective', () => {
  it('should create an instance', () => {
    const directive = new LinethroughDirective();
    expect(directive).toBeTruthy();
  });
});
